# Use H2 for tests
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# Flyway should run on H2 as well
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
# App Config
app.storage.root=/tmp/nas-storage-test
app.security.bootstrap-admin-password=test-admin-password
# Disable real JWT/Security for simple context loading if possible, or use defaults
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=3600000
security.vpn.allowed-subnets=0.0.0.0/0
